import { EnhancedFileManager } from "@/components/file-manager/enhanced-file-manager";

// Use the enhanced file manager as the default export
export default function FileManager() {
  return <EnhancedFileManager />;
}